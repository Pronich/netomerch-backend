name: demo-vars

on:
  push:
    branches: [ artrey-vars ]

jobs:
  vars:
    runs-on: ubuntu-latest
    steps:
      - name: print        
        env:
          GITHUB_CONTEXT: ${{ toJSON(github) }}
        run: echo "$GITHUB_CONTEXT"
